name: flutter_vocab_topik
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.3

  android_intent: ^2.0.2

  flutter_swipe_action_cell: ^2.1.0

  shared_preferences: ^2.0.6

  indexed_list_view: ^2.0.0

  sprintf: ^6.0.0

  url_launcher: ^6.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/files/

