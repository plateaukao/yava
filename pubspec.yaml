name: flutter_vocab_topik
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.1

  android_intent: ^0.3.7+7

  flutter_swipe_action_cell: ^1.3.4

  shared_preferences: ^0.5.12+4

  indexed_list_view: ^1.1.7

  sprintf: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/files/

